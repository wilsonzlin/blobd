{
  "name": "@turbostore/client",
  "version": "0.2.0",
  "main": "dist/main.js",
  "files": [
    "dist/**"
  ],
  "repository": "https://github.com/wilsonzlin/js",
  "author": "Wilson Lin <npm@wilsonl.in>",
  "license": "ISC",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "format": "prettier --ignore-path .gitignore --write '**/*.{js,json,ts,tsx}'",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "prettier": "2.5.1",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "@xtjs/lib": "^0.39.7"
  }
}
